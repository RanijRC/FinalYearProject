<SfDialog Width="350px" IsModal="true" ShowCloseIcon="true" @bind-Visible="IsVisible">
    <DialogEvents OnOpen="OpenDialog"></DialogEvents>
    <DialogTemplates>
        <Header>@Title Date Pending Type</Header>
        <Content>
            <EditForm Model="DataPendingType" Enhance OnSubmit="Save">
                <div class="card border-success">
                    <div class="card-body">
                        <div class="form-group">
                            <label class="form-label">Name</label>
                            <InputText @bind-Value="DataPendingType.Name" class="form-control" />
                        </div>
                    </div>
                </div>
            </EditForm>
        </Content>
    </DialogTemplates>

    <DialogButtons>
        <DialogButton Content="SAVE" Type="ButtonType.Submit" IsPrimary="true" OnClick="Save" />
    </DialogButtons>
</SfDialog>
@code {
    [Parameter] public DataPendingType DataPendingType { get; set; } = new();
    [Parameter] public EventCallback<DataPendingType> HandleSaveOperationEvent { get; set; }
    private bool IsVisible { get; set; } = false;
    private string Title { get; set; } = "Add";
    public void OpenDialog()
    {
        IsVisible = true;
        StateHasChanged();
    }

    async void Save()
    {
        if (string.IsNullOrEmpty(DataPendingType.Name))
            await dialogService.AlertAsync("You need to provide DataPendingType name.", "Alert!");
        else
            await HandleSaveOperationEvent.InvokeAsync(DataPendingType);
    }

    public void ChangeTitle(string title)
    {
        Title = title;
    }
}
